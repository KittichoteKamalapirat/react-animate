import React from "react";
import SectionWrapper from "../wrappers/SectionWrapper";

interface Props {}

const FormSection = ({}: Props) => {
  return (
    <SectionWrapper>
      <div>Add Embeded TypeForm</div>
    </SectionWrapper>
  );
};
export default FormSection;
