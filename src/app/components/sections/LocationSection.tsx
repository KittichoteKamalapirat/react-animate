import React from "react";
import SectionWrapper from "../wrappers/SectionWrapper";

interface Props {}

const LocationSection = ({}: Props) => {
  return (
    <SectionWrapper>
      <p>The Hall Laksi</p>
      <p>If mobile, open in Google map</p>
    </SectionWrapper>
  );
};
export default LocationSection;
